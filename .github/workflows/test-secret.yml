name: Test Secret

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secret length
        env:
          TOKEN: ${{ secrets.MIRROR_TOKEN }}
        run: |
          echo "Token length: ${#TOKEN}"
